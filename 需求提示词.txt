# Excel多空间智能图表分析工具 全栈开发正式需求提示词
## 项目类型：本地桌面Web应用，浏览器访问本地服务，最终打包单EXE免安装运行
## 开发准则：严格1:1实现所有需求，不得删减功能、不得修改业务逻辑，代码可直接运行调试

# 一、项目整体概述
开发一款基于Flask+Vue3的本地Excel数据分析可视化工具，支持多独立隔离工作空间，每个空间独立存储数据集、图表、AI对话记录；支持Excel上传、多工作表选择、数据清洗预处理、多类型可视化图表、线性回归趋势预测；内置多套OpenAI兼容大模型配置，空间可绑定独立AI，AI可读取表格数据智能数据分析问答；所有数据本地SQLite持久化存储，支持空间导出、数据库备份恢复，最终打包为单EXE一键启动自动打开浏览器。

# 二、强制指定技术栈
后端：Python3.9+、Flask、SQLAlchemy、pandas、numpy、sklearn、openpyxl、requests、plotly
前端：HTML5、Bootstrap5、Vue3、Axios、Plotly.js
数据库：SQLite本地数据库
打包工具：PyInstaller
运行模式：本地127.0.0.1内网服务

# 三、详细功能需求
## 3.1 空间管理
创建、删除、重命名、切换独立工作空间，删除空间级联清空所有关联数据

## 3.2 Excel数据集管理
支持xlsx/xls上传、多工作表选择、数据预览、自动识别字段类型；
内置完整数据预处理：缺失值多种处理方式、大数据量采样、日期字段转换；
数据集删除级联删除关联所有图表；限制单文件50MB上传

## 3.3 智能图表可视化
支持散点/折线/柱状/箱线/面积/饼图；自定义图表样式、坐标轴、标题；
支持开启线性回归趋势分析，自动计算斜率、截距、R²、趋势方向；
自动外推未来3期预测值，图表叠加趋势线与预测标记点；
支持图表编辑、删除、导出图片、导出分析数据CSV

## 3.4 AI智能数据分析助手
支持多组OpenAI兼容API配置管理（增删改查、默认配置）；
每个工作空间独立绑定指定AI配置；
聊天对话可绑定数据集，自动带入表格结构+样本数据+统计信息；
携带历史对话上下文，限制上下文条数防超限，设置调用超时；
聊天记录按空间隔离存储，支持清空对话、保存AI分析笔记

## 3.5 导出备份系统
单图表导出图片、趋势数据导出CSV；
整空间打包ZIP导出全部数据与图表；
全库数据库一键备份、备份文件恢复功能

## 3.6 系统安全与优化
可选启动访问密码；上传文件UUID重命名防路径攻击；
统一后端错误响应格式；全局日志记录错误与操作日志；
十万行以上大数据自动采样优化，海量点位图表启用WebGL渲染

# 四、数据库设计
严格按照以下数据表使用SQLAlchemy ORM实现：
1. space 空间表
2. dataset 数据集表
3. chart 图表配置表
4. ai_config AI模型配置表
5. space_ai_config 空间AI关联表
6. chat_history 空间聊天记录表
7. analysis_note 分析笔记表
所有外键配置级联删除，字段类型、默认值、存储JSON字段严格对齐需求

# 五、后端API接口
全部接口统一前缀/api，严格实现需求内所有RESTful接口，包含空间、数据集、图表、AI配置、聊天、导出、备份全套接口；
统一成功/失败JSON返回格式，完善参数校验与异常捕获

# 六、前端页面布局
采用三栏经典布局：左侧空间导航、中间主功能区、右侧AI聊天栏；
所有弹窗：上传弹窗、新建图表弹窗、AI配置弹窗、备份恢复弹窗完整实现；
使用Vue3 CDN引入、Bootstrap5样式、Axios请求、Plotly渲染交互式图表

# 七、核心业务算法
1. 完整线性回归拟合算法，兼容空值异常值过滤
2. 多种缺失值填充逻辑
3. 随机采样/等距采样大数据逻辑
4. 时间字段转数值轴转换逻辑
5. AI数据摘要自动拼接逻辑

# 八、EXE打包硬性规范
1. 代码做好MEIPASS打包路径兼容，区分开发环境与打包环境
2. 所有存储目录（上传文件、数据库、日志）默认生成在EXE同级目录
3. 程序启动自动调用默认浏览器打开本地访问地址
4. 关闭命令行同步终止后台服务
5. 使用PyInstaller打包为独立单文件exe

# 九、全局强制开发约束
1. 所有代码添加清晰中文注释
2. 禁止简化任意一项需求功能
3. 所有文件校验、权限校验、格式校验齐全
4. AI接口调用设置60秒超时，失败返回友好提示
5. 严格前后端分离，前端只做展示交互，业务逻辑全部放在后端

# 十、最终必须交付完整文件
1. app.py 完整后端Flask主程序
2. templates/index.html 全套前端页面代码
3. requirements.txt 完整依赖清单
4. build.bat Windows一键打包脚本
5. README.md 简洁使用部署手册
所有文件保证本地python app.py可直接运行无报错