# Excel多空间智能图表分析工具 — 全栈开发任务拆分文档

> 生成日期：2025-07-10
> 项目类型：本地桌面Web应用（Flask + Vue3 + SQLite + PyInstaller）

---

## 任务状态说明

| 状态标记 | 含义 |
|----------|------|
| ⬜ **待开始** | 任务尚未启动 |
| 🔄 **进行中** | 任务正在进行 |
| ✅ **已完成** | 任务开发完成，待验证 |
| ✅ **已验证** | 任务通过验证，确认完成 |
| ❌ **阻塞** | 任务因外部依赖受阻 |

---

## 第一阶段：项目骨架与环境搭建

### T-1.1 项目初始化与目录结构搭建
- **状态**：⬜ 待开始
- **描述**：创建完整的项目目录结构，包括后端、前端、数据存储等目录
- **子项**：
  1. 创建项目根目录 `excelAny/`
  2. 创建 `app.py` 入口文件
  3. 创建 `templates/index.html` 前端页面骨架
  4. 创建 `requirements.txt` 依赖清单
  5. 创建 `build.bat` 打包脚本占位
  6. 创建 `README.md` 占位
  7. 创建 `uploads/`、`exports/`、`backups/`、`logs/`、`data/` 存储目录
  8. 创建 `static/` 目录（CSS/JS/图片）
- **验证标准**：
  - 目录结构完整，每个文件夹内放 `.gitkeep` 占位
  - `python app.py` 可启动空服务无报错

### T-1.2 Flask 后端骨架搭建
- **状态**：⬜ 待开始
- **描述**：初始化 Flask 应用，配置基本中间件、统一错误处理、日志系统
- **子项**：
  1. 创建 Flask 应用实例，配置 SECRET_KEY
  2. 配置统一 JSON 返回格式 `{ "code": 0, "msg": "success", "data": {} }`
  3. 配置全局异常捕获（400/404/500）
  4. 配置 CORS 中间件（本地开发允许跨域）
  5. 初始化日志模块（logging），写入 `logs/app.log`
  6. 配置 SQLAlchemy 数据库连接（SQLite，路径 `data/excelany.db`）
  7. 配置 MEIPASS 打包路径兼容逻辑（判断 `sys._MEIPASS`）
  8. Blueprint 注册 `api` 统一前缀 `/api`
- **验证标准**：
  - 访问 `http://127.0.0.1:5000/api/health` 返回 `{"code":0,"msg":"success","data":"ok"}`
  - 日志文件自动生成在 `logs/app.log`

### T-1.3 数据库 ORM 模型定义
- **状态**：⬜ 待开始
- **描述**：严格按照需求定义7张数据表的 SQLAlchemy ORM 模型
- **子项**：
  1. **space 空间表**：id, name, created_at, updated_at
  2. **dataset 数据集表**：id, space_id(FK), name, original_filename, file_path, file_size, row_count, column_count, columns_info(JSON), sheets_list(JSON), created_at, updated_at — 外键级联删除
  3. **chart 图表配置表**：id, dataset_id(FK), space_id(FK), title, chart_type(bar/line/scatter/box/pie/area), config(JSON: 坐标轴/样式/颜色/回归参数), chart_image_path, created_at, updated_at — 外键级联删除
  4. **ai_config AI模型配置表**：id, name, api_base_url, api_key, model_name, model_list_cache(JSON: 缓存拉取的模型列表), max_tokens, temperature, timeout, is_default, created_at, updated_at
  5. **space_ai_config 空间AI关联表**：id, space_id(FK), ai_config_id(FK), created_at — 外键级联删除
  6. **chat_history 空间聊天记录表**：id, space_id(FK), role(user/assistant/system), content, dataset_id(FK nullable), extra_data(JSON), created_at — 外键级联删除
  7. **analysis_note 分析笔记表**：id, space_id(FK), title, content, dataset_id(FK nullable), chart_id(FK nullable), created_at, updated_at — 外键级联删除
- **验证标准**：
  - `db.create_all()` 在 `data/excelany.db` 正确生成7张表
  - 所有外键配置 `ondelete="CASCADE"`
  - 字段类型、默认值、可空约束正确

---

## 第二阶段：空间管理模块

### T-2.1 空间管理后端 API
- **状态**：⬜ 待开始
- **描述**：实现工作空间的增删改查及切换接口
- **子项**：
  1. `GET  /api/spaces` — 获取所有空间列表
  2. `POST /api/spaces` — 创建空间（参数：name）
  3. `PUT  /api/spaces/<id>` — 重命名空间（参数：name）
  4. `DELETE /api/spaces/<id>` — 删除空间（级联删除所有关联数据）
  5. `POST /api/spaces/<id>/activate` — 切换/激活当前空间
  6. 删除空间前校验空间是否存在，不存在返回 404
  7. 删除空间级联删除：dataset → chart → chat_history → analysis_note → space_ai_config
- **验证标准**：
  - 通过 API 可完成空间创建 → 列表查询 → 重命名 → 删除全流程
  - 删除空间后关联数据集等数据同步删除（数据库验证）

### T-2.2 空间管理前端界面
- **状态**：⬜ 待开始
- **描述**：实现左侧三栏布局的空间导航面板
- **子项**：
  1. 三栏布局：左侧空间导航(260px)、中间主内容区、右侧AI聊天栏(380px)
  2. 空间列表显示所有空间名称，当前空间高亮标记
  3. 创建空间：点击"新建空间"按钮 → 弹窗输入名称 → 提交
  4. 重命名空间：右键/编辑按钮 → 弹窗修改名称 → 提交
  5. 删除空间：点击删除按钮 → 确认弹窗（提示会删除所有关联数据）→ 提交
  6. 空间切换：点击空间卡片 → 高亮并加载该空间数据
- **验证标准**：
  - 左侧空间列表可正常展示、创建、重命名、删除、切换
  - 删除弹窗正确提示级联删除风险

---

## 第三阶段：Excel 数据集管理模块

### T-3.1 Excel 上传与多工作表选择后端
- **状态**：⬜ 待开始
- **描述**：实现 Excel 文件上传、多工作表选择、数据预览接口
- **子项**：
  1. `POST /api/datasets/upload` — 上传文件（参数：file, space_id）
     - 限制 `.xlsx` / `.xls` 格式
     - 限制单文件 50MB
     - UUID 重命名保存，防路径攻击
     - 使用 openpyxl 解析，获取工作表列表及各表行列数
  2. `GET  /api/datasets/<id>/sheets` — 获取工作表列表
  3. `POST /api/datasets/<id>/sheet` — 选择工作表（参数：sheet_name）
     - 加载该工作表数据，自动识别字段类型（数值/文本/日期）
     - 数据预览仅返回前100行，前端分页渲染
  4. `GET  /api/datasets/<id>/preview?page=1&page_size=50` — 分页数据预览
- **验证标准**：
  - 上传 10MB / 50MB / 51MB 文件，边界测试正确
  - 上传后文件 UUID 重命名，原始文件名存入数据库
  - 多工作表文件可正确返回列表并切换

### T-3.2 超大批量数据专项适配
- **状态**：⬜ 待开始
- **描述**：实现海量数据的分块读取、分页预览、数据量判定与前置提醒
- **子项**：
  1. 数据量级判定函数：
     - 万行级：1万-10万行
     - 十万行级：10万-100万行
     - 百万行级：≥100万行
  2. 百万行级上传前置弹窗提醒：
     - 后端返回数据量级标识 + 预估耗时提示文案
     - 前端弹窗显示"该文件含 XX 行数据，为大文件，加载预计耗时 XX 秒，是否继续？"
  3. 分块读取模式：
     - 使用 `openpyxl.iter_rows()` / `pd.read_excel(chunksize=50000)` 分块
     - 禁止一次性 `pd.read_excel()` 全量载入
  4. 分页预览：
     - 后端只返回当前页数据，不缓存全部数据在内存
     - 前端分页组件：上一页/下一页/跳转/每页条数选择
  5. 数据清洗、统计运算均采用分片批量计算：
     - 分批处理，每批 50000 行
     - 处理期间释放中间变量内存（del + gc.collect）
  6. 后端内存占用监控：
     - 使用 `psutil` 或 `resource` 监控进程内存
     - 内存超过 80% 总配额时自动触发提示
     - 响应头/body 添加 `memory_warning` 字段
  7. 导出超大报表时异步处理 + 前端进度条
- **验证标准**：
  - 上传 120万行测试 Excel，不触发 OOM
  - 预览加载首屏 < 2秒（不计上传时间）
  - 内存占用不超过 500MB（100万行级别）
  - 分页切换流畅

### T-3.3 数据预处理后端
- **状态**：⬜ 待开始
- **描述**：实现缺失值处理、日期字段转换、数据采样等预处理接口
- **子项**：
  1. `POST /api/datasets/<id>/preprocess` — 数据预处理入口
  2. 缺失值处理：
     - 删除含缺失值的行
     - 填充均值/中位数/众数/指定值（数值列）
     - 填充前向填充/后向填充（时间序列）
     - 填充"未知"/"无"（文本列）
  3. 日期字段转换：
     - 自动识别日期格式（yyyy-mm-dd, yyyy/mm/dd, yyyy年mm月dd日等）
     - 转换为数值轴（时间戳）用于图表
  4. 数据采样（多模式）：
     - 随机采样：`df.sample(n=...)`
     - 等距采样：每隔 N 行取一行
     - 分层采样：按指定列分层等比采样
  5. 字段类型信息更新（预处理后重新识别）
- **验证标准**：
  - 含缺失值的测试数据集，每种填充模式结果正确
  - 日期列正确识别并转换
  - 三种采样模式结果符合预期

### T-3.4 数据集管理前端
- **状态**：⬜ 待开始
- **描述**：实现数据集上传、预览、预处理界面
- **子项**：
  1. 上传弹窗：文件选择 + 拖拽上传 + 50MB限制提示 + 上传进度条
  2. 上传后自动弹出工作表选择下拉框
  3. 数据预览表格（带分页组件）
  4. 字段类型信息展示（列名+类型标识）
  5. 数据预处理弹窗：
     - 缺失值处理：选择列 → 选择处理方式 → 确认
     - 日期转换：选择列 → 确认转换
     - 采样：选择模式 + 参数（采样数/比例）→ 确认
  6. 大数据前置提醒弹窗（数据量 ≥ 10万行时弹出）
  7. 内存过载警告提示组件
- **验证标准**：
  - 上传→选工作表→预览数据→预处理 全流程可用
  - 大数据弹窗在 ≥ 100万行时正常弹出
  - 分页正常，大数据分页切换 < 1秒

---

## 第四阶段：智能图表可视化模块

### T-4.1 图表创建与配置后端 API
- **状态**：⬜ 待开始
- **描述**：实现图表创建、配置、数据查询接口
- **子项**：
  1. `POST /api/charts` — 创建图表
     - 参数：space_id, dataset_id, title, chart_type, config(JSON)
     - config 包含：x_column, y_column, color_column, chart_style, axis_config, regression_config
  2. `GET  /api/charts?space_id=xxx` — 获取空间下的图表列表
  3. `GET  /api/charts/<id>` — 获取图表详情及数据
     - 返回图表配置 + 渲染数据（经过采样策略处理）
  4. `PUT  /api/charts/<id>` — 更新图表配置
  5. `DELETE /api/charts/<id>` — 删除图表
  6. 海量点位数据自动多级采样：
     - > 10万点 → 等距采样保留 50000 点
     - > 50万点 → 随机采样保留 30000 点
     - > 100万点 → 分层采样保留 20000 点
     - 采样在服务端进行，前端只接收采样后数据
     - 原始数据完整保留在数据库
  7. 图表数据查询强制 WebGL 标记（前端渲染时生效）
- **验证标准**：
  - 创建 6 种类型图表均成功，返回正确数据
  - 100万行数据集创建图表，返回数据量 ≤ 20000 条
  - 采样前后原始数据不变

### T-4.2 线性回归趋势分析
- **状态**：⬜ 待开始
- **描述**：实现线性回归拟合、评估、预测接口
- **子项**：
  1. 完整线性回归拟合（基于 sklearn.linear_model.LinearRegression）
     - 过滤 NaN/Inf 异常值
     - 适配海量数据分片运算
  2. 回归结果计算：
     - 斜率（slope）
     - 截距（intercept）
     - R² 决定系数
     - 趋势方向（上升/下降/平稳）
  3. 未来 3 期预测值自动外推
  4. 图表叠加趋势线（line trace） + 预测标记点（marker trace）
  5. 返回格式：`{ "regression": { "slope":..., "intercept":..., "r2":..., "trend":... }, "prediction": [ { "x":..., "y":..., "label":"预测1" }, ... ] }`
- **验证标准**：
  - 对线性数据 + 含噪声数据，回归结果合理
  - R² 值在 0~1 之间
  - 未来3期预测值趋势合理
  - 趋势线叠加在图表上正确显示

### T-4.3 图表编辑、导出功能
- **状态**：⬜ 待开始
- **描述**：实现图表编辑、删除、导出图片、导出CSV接口
- **子项**：
  1. `PUT /api/charts/<id>` — 编辑图表（标题、类型、配置）
  2. `DELETE /api/charts/<id>` — 删除图表（级联删除关联笔记）
  3. `POST /api/charts/<id>/export-image` — 导出图表图片
     - 使用 plotly.io.write_image 生成 PNG 图片
     - 保存至 `exports/` 目录，返回下载链接
  4. `POST /api/charts/<id>/export-csv` — 导出分析数据 CSV
     - 包含原始数据+拟合值+残差+预测值
  5. 超大图表的导出后台异步处理+进度条
- **验证标准**：
  - 编辑后图表正确更新
  - 导出 PNG 图片可正常打开
  - 导出 CSV 包含完整分析数据

### T-4.4 图表可视化前端
- **状态**：⬜ 待开始
- **描述**：实现图表创建弹窗、图表展示、编辑删除交互界面
- **子项**：
  1. 新建图表弹窗：
     - 选择数据集 → 选择X轴/Y轴列 → 选择图表类型(6种)
     - 自定义标题、坐标轴标签、颜色主题
     - 开启/关闭线性回归开关
  2. 图表渲染区（使用 Plotly.js）：
     - 正确渲染 6 种图表类型
     - 回归趋势线渲染为虚线 + 预测点
     - 海量点位自动启用 WebGL（`scattergl` / `scattergeo`）
  3. 图表卡片列表展示（缩略图 + 标题 + 类型图标）
  4. 图表编辑：点击编辑按钮 → 弹窗修改配置
  5. 图表删除：点击删除 → 确认弹窗
  6. 导出按钮：导出图片 / 导出CSV
  7. 大数据图表加载进度提示
- **验证标准**：
  - 6 种图表类型均能正确创建和渲染
  - 回归趋势线 + 预测点正确显示
  - WebGL 渲染在多点位时自动启用

---

## 第五阶段：AI 智能数据分析助手模块

### T-5.1 AI 配置管理后端 API
- **状态**：⬜ 待开始
- **描述**：实现 AI 模型配置的增删改查及默认配置管理
- **子项**：
  1. `GET  /api/ai-configs` — 获取所有 AI 配置列表
  2. `POST /api/ai-configs` — 新增 AI 配置
     - 参数：name, api_base_url, api_key, model_name, max_tokens, temperature, timeout
  3. `PUT  /api/ai-configs/<id>` — 更新 AI 配置
  4. `DELETE /api/ai-configs/<id>` — 删除 AI 配置
  5. `POST /api/ai-configs/<id>/set-default` — 设为默认配置
  6. 新增接口 — `POST /api/ai-configs/<id>/refresh-models` — 刷新拉取线上模型列表
  7. `GET  /api/ai-configs/<id>/cached-models` — 获取本地缓存模型列表
- **验证标准**：
  - 增删改查完整可用
  - 可设置默认配置

### T-5.2 模型刷新核心功能（新增重点）
- **状态**：⬜ 待开始
- **描述**：实现线上模型列表拉取、缓存、刷新接口
- **子项**：
  1. 后端刷新接口 `POST /api/ai-configs/<id>/refresh-models`：
     - 请求目标 API 的 `/v1/models` 或 `/models` 端点
     - 使用 requests 库，设置独立超时（默认30秒）
     - 解析返回 JSON，提取 `data[].id` 作为模型名称
     - 去重处理（过滤同名模型）
     - 更新 `ai_config.model_list_cache` JSON 字段
     - 返回最新模型列表
     - 失败时返回网络/接口异常提示，不覆盖原有缓存
  2. 前端刷新按钮：
     - AI 配置编辑页面新增【刷新模型】按钮
     - 空间 AI 绑定选择页面新增【刷新模型】按钮
     - 点击后发送请求 → 按钮显示加载旋转动画
     - 成功：弹窗提示"模型列表刷新成功！共获取 X 个模型"
     - 失败：弹窗提示"刷新失败：XX"，保留原有模型列表
  3. 本地缓存机制：
     - 刷新后的模型列表存入 `model_list_cache` 字段
     - 优先展示缓存列表，减少重复请求
     - 缓存 TTL：30分钟（可选），超时后自动刷新提示
  4. 定时静默刷新模式（可选）：
     - 页面初始化时检查缓存是否过期
     - 过期时后台静默刷新，不阻塞界面
- **验证标准**：
  - 调用刷新接口成功拉取线上模型列表
  - 缓存正确存储和读取
  - 刷新失败原地缓存不被清空

### T-5.3 空间 AI 绑定后端 API
- **状态**：⬜ 待开始
- **描述**：实现工作空间与 AI 配置的绑定管理
- **子项**：
  1. `POST /api/spaces/<id>/bind-ai` — 绑定 AI（参数：ai_config_id）
  2. `DELETE /api/spaces/<id>/unbind-ai` — 解绑 AI
  3. `GET  /api/spaces/<id>/bound-ai` — 获取当前空间绑定的 AI 配置
- **验证标准**：
  - 绑定/解绑/查询 完整可用
  - 一个空间同一时间只绑定一个 AI

### T-5.4 AI 智能问答后端 API
- **状态**：⬜ 待开始
- **描述**：实现 AI 数据智能分析问答接口
- **子项**：
  1. `POST /api/spaces/<id>/chat` — 发送消息
     - 参数：message, dataset_id(可选)
     - 自动带入数据集结构信息（列名+类型+前5行样本+统计信息）
     - 携带历史对话上下文（最近 N 条，N 可配置，默认 20 条）
     - 调用 OpenAI 兼容 API（/v1/chat/completions）
     - 设置 60 秒超时
     - 返回 AI 回复内容
  2. `GET  /api/spaces/<id>/chat/history` — 获取聊天记录
  3. `DELETE /api/spaces/<id>/chat/clear` — 清空聊天记录
  4. 数据集摘要自动拼接逻辑：
     - 表格结构：列名 | 类型 | 非空率 | 唯一值数
     - 样本数据：前 5 行完整数据
     - 统计信息：数值列 min/max/mean/std，文本列频次 top5
- **验证标准**：
  - 发送消息后正确返回 AI 回复
  - 绑定数据集时正确拼接数据结构信息
  - 上下文携带历史对话正常

### T-5.5 AI 聊天分析与笔记前端
- **状态**：⬜ 待开始
- **描述**：实现右侧 AI 聊天栏、消息展示、笔记保存界面
- **子项**：
  1. 右侧 AI 聊天栏（固定宽度 380px）
  2. AI 配置选择区：
     - 下拉框选择已配置的 AI
     - 【刷新模型】按钮醒目放置
     - 显示当前绑定状态
  3. 聊天消息展示：
     - 用户消息右对齐（蓝色气泡）
     - AI 回复左对齐（灰色气泡）
     - Markdown 渲染支持（代码块、表格、列表）
     - 加载中显示打字动画
  4. 输入框 + 发送按钮
  5. 数据集绑定选择器（可绑定当前空间下任意数据集）
  6. 清空对话按钮（需确认弹窗）
  7. 保存为分析笔记按钮
  8. AI 配置管理弹窗（添加/编辑/删除 AI 配置）
  9. 模型刷新按钮及刷新结果弹窗
- **验证标准**：
  1. 聊天发送/回复正常显示
  2. 配置管理、刷新、绑定全流程可用
  3. 保存笔记功能正常

---

## 第六阶段：导出备份系统

### T-6.1 图表导出后端
- **状态**：⬜ 待开始
- **描述**：实现单图表导出图片和分析数据 CSV 接口
- **子项**：
  1. `POST /api/charts/<id>/export-image` — 导出图片
     - 使用 plotly.io.write_image 生成 PNG
     - 文件名格式：`图表标题_时间戳.png`
  2. `POST /api/charts/<id>/export-csv` — 导出 CSV
     - 包含：原始数据、拟合值、残差、预测值（如有回归）
  3. 返回下载 URL 或直接返回文件流
- **验证标准**：
  - 导出 PNG 清晰可读
  - 导出 CSV 数据完整

### T-6.2 空间导出（ZIP 打包）
- **状态**：⬜ 待开始
- **描述**：实现整空间打包导出接口
- **子项**：
  1. `POST /api/spaces/<id>/export` — 导出空间
     - 打包内容：空间信息 JSON + 数据集文件 + 图表图片 + 聊天记录 JSON
     - 使用 zipfile 打包
     - 文件名格式：`空间名_时间戳.zip`
  2. 超大空间异步打包 + 进度查询：
     - `GET /api/spaces/<id>/export/progress` — 查询打包进度
  3. 返回下载 URL
- **验证标准**：
  - 导出的 ZIP 包含完整数据
  - 异步打包进度可查询

### T-6.3 数据库备份恢复
- **状态**：⬜ 待开始
- **描述**：实现全库备份与恢复功能
- **子项**：
  1. `POST /api/backup` — 创建备份
     - 使用 shutil.copy 复制 `excelany.db` 到 `backups/` 目录
     - 文件名格式：`excelany_backup_时间戳.db`
  2. `GET  /api/backups` — 获取备份列表
  3. `POST /api/backups/<id>/restore` — 恢复备份
     - 关闭当前数据库连接
     - 替换数据库文件
     - 重新初始化数据库连接
     - 返回恢复结果
  4. `DELETE /api/backups/<id>` — 删除备份文件
- **验证标准**：
  - 备份文件正确生成
  - 恢复后数据完整
  - 恢复后服务正常

### T-6.4 导出备份前端
- **状态**：⬜ 待开始
- **描述**：实现导出备份相关前端界面和交互
- **子项**：
  1. 图表导出按钮（图片/CSV）
  2. 空间导出按钮 → 确认弹窗 → 进度条 → 下载
  3. 备份恢复弹窗：
     - 备份列表展示
     - 创建备份按钮
     - 恢复备份按钮（需确认弹窗）
     - 删除备份按钮（需确认弹窗）
  4. 异步任务进度条组件（全局通用）
- **验证标准**：
  - 所有导出/备份操作弹窗正常
  - 进度条动态更新
  - 下载功能正常

---

## 第七阶段：系统安全与优化

### T-7.1 全局安全与错误处理
- **状态**：⬜ 待开始
- **描述**：实现启动密码、文件校验、统一错误响应
- **子项**：
  1. 可选启动访问密码：
     - 启动时可通过命令行参数 `--password=xxx` 设置
     - 未设置密码直接进入，设置密码后访问首页需输入密码
     - 密码校验接口 `POST /api/auth/check-password`
  2. 上传文件 UUID 重命名
  3. 统一错误响应格式：`{ "code": error_code, "msg": "错误描述", "data": null }`
  4. 全局日志记录：
     - 操作日志（创建/删除/修改操作）
     - 错误日志（异常堆栈）
     - 日志文件按天分割
- **验证标准**：
  - 设置密码后访问被拦截
  - UUID 文件名无原始路径信息
  - 异常返回统一格式

### T-7.2 EXE 程序退出逻辑专项优化（重点）
- **状态**：⬜ 待开始
- **描述**：实现浏览器关闭不终止、一键退出、强制退出兜底的完整退出逻辑
- **子项**：
  1. 常规关闭优化：
     - 浏览器关闭网页时，Flask 服务不终止
     - 使用 `threading` 后台运行，非 `app.run(debug=True)` 模式
     - 再次打开浏览器可继续使用
  2. 一键彻底退出功能：
     - 前端页面显眼位置放置【退出程序】按钮
     - 点击后弹出确认弹窗："是否确认退出程序？未保存的数据可能会丢失，建议先备份数据库。"
     - 确认后前端发送 `POST /api/system/shutdown` 请求
     - 后端接收后：
       a. 关闭所有数据库连接
       b. 释放所有文件读写占用
       c. 释放 AI 请求连接
       d. 清空临时缓存
       e. 调用 `os._exit(0)` 或 `sys.exit()` 关闭进程
       f. 自动关闭命令行窗口（通过 Windows API）
  3. 强制退出兜底：
     - 监听 `signal.SIGINT` / `signal.SIGTERM` 信号
     - 用户直接关闭 CMD 黑框时触发
     - 自动执行资源释放 → 数据库正常关闭 → 缓存清空
     - 避免数据库损坏、文件占用、进程残留
  4. 退出前自动弹窗确认（前端实现）：
     - 检测是否有未保存数据
     - 提示用户备份数据库
  5. 两种模式自由切换：后台静默驻留 / 一键彻底退出
- **验证标准**：
  - 关闭浏览器 → 服务仍在运行，重新打开可用
  - 点击退出按钮 → 服务进程完全终止，无残留
  - 直接关闭黑框 → 资源正常释放，数据库无损坏

---

## 第八阶段：EXE 打包与部署

### T-8.1 PyInstaller 打包配置
- **状态**：⬜ 待开始
- **描述**：编写打包脚本，配置 PyInstaller 参数
- **子项**：
  1. 编写 `build.bat` 打包脚本：
     ```bat
     pyinstaller --onefile --name=ExcelAny --add-data "templates;templates" --add-data "static;static" --distpath ./dist --workpath ./build --specpath ./ app.py
     ```
  2. MEIPASS 路径兼容：
     - `if getattr(sys, 'frozen', False): base_path = sys._MEIPASS`
     - 否则 `base_path = os.path.abspath(".")`
  3. 所有存储目录（uploads/exports/backups/logs/data）默认生成在 EXE 同级目录
  4. 程序启动自动调用默认浏览器打开 `http://127.0.0.1:5000`
  5. 打包时包含大数据依赖：numpy, pandas, sklearn, plotly
- **验证标准**：
  - `build.bat` 执行成功，生成 `dist/ExcelAny.exe`
  - EXE 启动后浏览器自动打开
  - 存储目录生成在 EXE 同级

### T-8.2 README 与文档
- **状态**：⬜ 待开始
- **描述**：编写 README.md 使用手册
- **子项**：
  1. 项目简介
  2. 环境要求（Python 3.9+）
  3. 开发运行方式：`pip install -r requirements.txt` → `python app.py`
  4. 打包运行方式：双击 `build.bat` → 运行 `dist/ExcelAny.exe`
  5. 海量数据使用注意事项：
     - 建议内存 ≥ 8GB
     - 百万级数据加载需等待
     - 上传前建议精简数据列
  6. 程序正确退出方式：
     - 点击界面【退出程序】按钮
     - 或直接关闭命令行窗口（自动清理资源）
  7. 功能模块说明
- **验证标准**：
  - README 内容完整，markdown 格式正确

---

## 第九阶段：集成测试与验证

### T-9.1 全流程集成测试
- **状态**：⬜ 待开始
- **描述**：执行完整端到端测试，验证所有功能
- **子项**：
  1. 空间管理全流程测试
  2. Excel 上传（含超大文件）全流程测试
  3. 数据预处理全流程测试
  4. 6种图表创建+回归分析全流程测试
  5. AI 配置管理+模型刷新+聊天对话全流程测试
  6. 导出备份全流程测试（图片/CSV/ZIP/数据库）
  7. 程序退出功能测试（网页关闭/一键退出/强制退出）
  8. 内存监控与大数据采样正确性验证
  9. 安全校验测试（密码/文件格式/50MB限制）
- **验证标准**：
  - 所有功能按需求1:1实现，无遗漏
  - 无报错、无卡死、无内存溢出

### T-9.2 打包验证
- **状态**：⬜ 待开始
- **描述**：验证 PyInstaller 打包后的 EXE 正常运行
- **子项**：
  1. 在干净 Windows 环境执行打包
  2. 运行 EXE，验证浏览器自动打开
  3. 所有功能在打包环境下正常运行
  4. 退出功能正常
- **验证标准**：
  - EXE 单文件可运行
  - 所有功能在 EXE 模式下正常

---

## 任务依赖关系图

```
T-1.1 项目初始化 ──→ T-1.2 Flask骨架 ──→ T-1.3 数据库模型
                                              │
                ┌──────────────────────────────┤
                │                              │
                ▼                              ▼
         T-2 空间管理模块               T-3 数据集管理模块
                │                              │
                ├──────────────────────────────┤
                │                              │
                ▼                              ▼
         T-4 图表可视化模块 ────→ T-6.1/6.2 导出功能
                │
                ▼
         T-5 AI智能分析模块
                │
                ▼
         T-6 导出备份系统
                │
                ▼
         T-7 系统安全与优化
                │
                ▼
         T-8 EXE打包与部署
                │
                ▼
         T-9 集成测试与验证
```

## 并行开发建议

| 阶段 | 可并行任务 |
|------|-----------|
| 第一阶段 | T-1.1 + T-1.2 + T-1.3 建议顺序执行 |
| 第二阶段 | T-2.1(后端) + T-2.2(前端) 可并行 |
| 第三阶段 | T-3.1 + T-3.3(后端) 可并行；T-3.2 + T-3.4(前端) 可并行 |
| 第四阶段 | T-4.1 + T-4.2(后端) 可并行；T-4.3 + T-4.4(前端) 可并行 |
| 第五阶段 | T-5.1 + T-5.2 + T-5.3(后端) 可并行；T-5.4 + T-5.5(前端) 可并行 |
| 第六阶段 | T-6.1 + T-6.2 + T-6.3(后端) 可并行；T-6.4(前端) 后续 |
| 第七阶段 | T-7.1 + T-7.2 建议顺序执行 |
| 第八阶段 | 所有后端任务完成后 |
| 第九阶段 | 所有开发任务完成后 |
