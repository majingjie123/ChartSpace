# Excel 多空间智能图表分析工具 - 任务计划

> 生成日期：2026-05-20
> 项目目录：D:\code\other\excelAny

---

## 一、项目概述

基于 Web 的桌面应用工具，支持多空间（工作区）隔离管理，每个空间内可上传 Excel 创建多种图表、进行趋势分析，内置 AI 助手（支持多个 OpenAI 兼容 API 配置），所有数据本地 SQLite 持久化存储，最终打包为单个 .exe 文件。

### 技术栈

- **后端**：Python 3.9+, Flask, SQLAlchemy, pandas, numpy, scikit-learn, openpyxl, requests, plotly
- **前端**：HTML5, CSS3 (Bootstrap 5), JavaScript (Vue 3 CDN), Axios, Plotly.js
- **数据库**：SQLite (SQLAlchemy ORM)
- **打包**：PyInstaller

---

## 二、任务状态总览

> 更新时间：2026-05-20
> 图例：⬜ 未开始 | 🔄 进行中 | ✅ 已完成 | 🧪 已验证

### 第一层：后端基础（任务 1-2）

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 1 | 项目初始化与基础架构 | 最高 | ⬜ 未开始 |
| 2 | 数据库模型定义（7张表） | 最高 | ⬜ 未开始 |

### 第二层：后端 API（任务 3-11）

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 3 | 空间管理 CRUD | 高 | ⬜ 未开始 |
| 4 | 数据集管理（上传/预览/预处理） | 高 | ⬜ 未开始 |
| 5 | 图表管理（6种类型/渲染/编辑） | 高 | ⬜ 未开始 |
| 6 | 趋势分析（线性回归/预测） | 高 | ⬜ 未开始 |
| 7 | AI 配置管理 | 高 | ⬜ 未开始 |
| 8 | AI 聊天（绑定/对话/历史） | 高 | ⬜ 未开始 |
| 9 | 分析笔记 API | 中 | ⬜ 未开始 |
| 10 | 导出与备份（ZIP/DB） | 中 | ⬜ 未开始 |
| 11 | 安全与错误处理 | 高 | ⬜ 未开始 |

### 第三层：前端界面（任务 12-20）

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 12 | 前端基础框架（Vue3+布局） | 高 | ⬜ 未开始 |
| 13 | 左侧边栏（空间列表/AI配置入口） | 高 | ⬜ 未开始 |
| 14 | 中间主区域（标题/数据集） | 高 | ⬜ 未开始 |
| 15 | 中间图表区（图表卡片/新建图表） | 高 | ⬜ 未开始 |
| 16 | 趋势分析面板（展示/CSV导出） | 高 | ⬜ 未开始 |
| 17 | 右侧 AI 助手面板（聊天界面） | 高 | ⬜ 未开始 |
| 18 | 分析笔记面板 | 中 | ⬜ 未开始 |
| 19 | 模态框（AI配置/备份恢复） | 中 | ⬜ 未开始 |
| 20 | 大数据优化（WebGL/采样提示） | 中 | ⬜ 未开始 |

### 第四层：打包与测试（任务 21-22）

| # | 任务 | 优先级 | 状态 |
|---|------|--------|------|
| 21 | PyInstaller 打包（build.bat/exe） | 高 | ⬜ 未开始 |
| 22 | 集成测试与验证（10个场景） | 高 | ⬜ 未开始 |

**进度汇总**：⬜ 22 未开始 | 0  进行中 | 0  已完成 | 0  已验证 | **0%**

---

## 三、详细任务清单（共 22 项）

### 第一层：后端基础

#### 任务 1：项目初始化与基础架构

| 字段 | 内容 |
|------|------|
| **优先级** | 最高 |
| **状态** | 未开始 |
| **工作量预估** | 中 |
| **详细描述** | 创建项目目录结构（`templates/`、`uploads/`、`logs/`、`backup/`），生成 `requirements.txt` 列出全部依赖包及版本号，创建 `app.py` 骨架包含 Flask 应用初始化、配置加载（PORT、SECRET_KEY、UPLOAD_FOLDER 等）、SQLite 数据库 URI、启动入口 `if __name__ == '__main__'`。 |
| **验收标准** | `python app.py` 能启动 Flask 开发服务器（默认 5000 端口），访问 `http://127.0.0.1:5000` 返回 404 而非报错 |
| **依赖** | 无 |
| **产出文件** | `requirements.txt`, `app.py`, `templates/`, `uploads/`, `logs/`, `backup/` |

#### 任务 2：数据库模型定义

| 字段 | 内容 |
|------|------|
| **优先级** | 最高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | 使用 SQLAlchemy ORM 定义 7 张表的 Model 类：`Space`、`Dataset`、`Chart`、`AIConfig`、`SpaceAIConfig`、`ChatHistory`、`AnalysisNote`。需实现：(1) 所有外键及 `ON DELETE CASCADE` 约束；(2) `Dataset.preprocessing_options` 和 `Chart.config` 的 JSON 序列化/反序列化辅助方法；(3) 每个 Model 的 `to_dict()` 方法；(4) `init_db()` 函数建表。 |
| **验收标准** | 启动时自动建表，SQLite 文件正确生成且包含全部 7 张表结构 |
| **依赖** | 任务 1 |
| **产出文件** | `app.py` 中的 Model 定义部分 |

---

### 第二层：后端 API

#### 任务 3：空间管理 API

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | 实现 `/api/spaces` 的 4 个端点：(1) `GET` 获取所有空间列表（按创建时间倒序）；(2) `POST` 创建空间（body: `{ "name": "新空间" }`）；(3) `PUT /api/spaces/<id>` 重命名；`DELETE /api/spaces/<id>` 删除（级联删除关联数据集、图表、聊天记录）。返回统一 JSON 格式。 |
| **验收标准** | 使用 Postman/curl 测试 4 个端点均正常工作，删除空间后关联数据被清除 |
| **依赖** | 任务 2 |
| **产出文件** | `app.py` 中的空间蓝图路由 |

#### 任务 4：数据集管理 API

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) `POST /api/spaces/<id>/datasets` 上传 Excel（multipart/form-data），限制 50MB，UUID 重命名文件，使用 openpyxl 读取。(2) `GET /api/datasets/<id>/sheets` 返回工作表名列表。(3) `POST /api/datasets/<id>/upload-confirm` 确认选择的工作表，保存到 DB。(4) `GET /api/datasets/<id>/preview?rows=100` 返回前 N 行数据及列类型（数值/日期/文本）。(5) `POST /api/datasets/<id>/preprocess` 应用预处理（缺失值策略：删除行/向前填充/线性插值/填充均值或中位数；采样策略：随机采样 N 行/等距采样；日期列转时间戳）。(6) `DELETE /api/datasets/<id>` 删除数据集及其图表。 |
| **验收标准** | 上传 xlsx 文件 → 获取工作表名 → 选择工作表 → 预览数据 → 预处理 → 删除，全流程可正常工作 |
| **依赖** | 任务 2, 3 |
| **产出文件** | `app.py` 中的数据集蓝图路由 |

#### 任务 5：图表管理 API

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) `GET /api/spaces/<id>/charts` 获取空间内图表列表。(2) `POST /api/spaces/<id>/charts` 创建图表（body: `{ dataset_id, name, chart_type, x_col, y_col, y2_col, trend_enabled, config }`）。(3) `PUT /api/charts/<id>` 更新图表。(4) `DELETE /api/charts/<id>` 删除。(5) `GET /api/charts/<id>/render` 核心接口：读取数据集 → 根据 chart_type 构建 Plotly 图表 JSON → 如果 trend_enabled 则调用趋势分析模块叠加趋势线 → 返回 `{ "chart_html": "...", "trend_info": {...} }`。(6) `POST /api/charts/<id>/export-image` 输出 PNG/PDF。 |
| **验收标准** | 创建 6 种类型图表均能正确渲染返回 HTML，编辑和删除正常 |
| **依赖** | 任务 4 |
| **产出文件** | `app.py` 中的图表蓝图路由 |

#### 任务 6：趋势分析模块

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | 独立的 `trend_analysis(x, y)` 函数：(1) 清理 NaN/Inf 值；(2) 使用 sklearn.linear_model.LinearRegression 拟合；(3) 计算 R² (r2_score)；(4) 计算斜率、截距；(5) 判断趋势方向（斜率 > 0.01 上升，< -0.01 下降，否则平稳）；(6) 外推未来 3 期预测值（基于最后一个数据点的步长）。返回值实现为 `TrendResult` 命名元组。 |
| **验收标准** | 对已知线性数据（y=2x+1）输入，返回斜率=2、R²=1、截距=1 |
| **依赖** | 任务 5（被图表渲染调用） |
| **产出文件** | `app.py` 中的 `trend_analysis()` 函数 |

#### 任务 7：AI 配置管理 API

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | (1) `GET /api/ai-configs` 获取所有配置；(2) `POST /api/ai-configs` 新增（name, base_url, api_key, model, system_prompt, max_tokens, temperature）；(3) `PUT /api/ai-configs/<id>` 更新；(4) `DELETE /api/ai-configs/<id>` 删除；(5) `POST /api/ai-configs/<id>/set-default` 设置默认（取消其他配置的 is_default）。返回统一 JSON。 |
| **验收标准** | CRUD 4 个操作均正常，设置默认后数据库中只有一条记录的 is_default=1 |
| **依赖** | 任务 2 |
| **产出文件** | `app.py` 中的 AI 配置蓝图路由 |

#### 任务 8：AI 聊天 API

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) `GET /api/spaces/<id>/ai-config` 获取空间绑定的 AI 配置 ID。(2) `POST /api/spaces/<id>/ai-config` 设置绑定（body: `{ ai_config_id }`）。(3) `POST /api/chat` 核心接口：接收 `{ space_id, message, dataset_id? }` → 读取空间绑定的 AI 配置 → 若传入 dataset_id 则读取该数据集摘要（列名、前 10 行、describe() 统计）拼接到 system prompt → 从 chat_history 取最近 20 条消息构建 messages 数组 → 调用 OpenAI 兼容 API（`{base_url}/chat/completions`，超时 60s）→ 将用户消息和 AI 回复存入 DB → 返回 `{ "reply": "..." }`。(4) `GET /api/spaces/<id>/chat-history` 获取历史。(5) `DELETE /api/spaces/<id>/chat-history` 清空。 |
| **验收标准** | 发送消息后 AI 返回正常回复（需有真实 API 或 Mock），历史记录正确存储和展示 |
| **依赖** | 任务 4, 7 |
| **产出文件** | `app.py` 中的聊天蓝图路由 |

#### 任务 9：分析笔记 API

| 字段 | 内容 |
|------|------|
| **优先级** | 中 |
| **状态** | 未开始 |
| **详细描述** | (1) `POST /api/notes` 保存笔记（body: `{ space_id, title, content }`）；(2) `GET /api/spaces/<id>/notes` 获取空间所有笔记；(3) `DELETE /api/notes/<id>` 删除。 |
| **验收标准** | 保存和获取笔记正常 |
| **依赖** | 任务 2 |
| **产出文件** | `app.py` 中的笔记蓝图路由 |

#### 任务 10：导出与备份 API

| 字段 | 内容 |
|------|------|
| **优先级** | 中 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) `GET /api/spaces/<id>/export` 导出空间 ZIP：读取空间下所有数据集原始 Excel 文件 → 为每个图表生成 HTML 快照（使用 Plotly 离线模式）→ 生成趋势分析 CSV → 生成聊天记录 JSON → 打包 ZIP 返回。(2) `GET /api/system/backup` 下载 SQLite 数据库文件。(3) `POST /api/system/restore` 上传备份的数据库文件覆盖当前数据库。 |
| **验收标准** | 空间 ZIP 下载包含所有数据文件，备份文件可下载，恢复后数据一致 |
| **依赖** | 任务 4, 5, 6, 8 |
| **产出文件** | `app.py` 中的导出备份蓝图路由 |

#### 任务 11：安全与错误处理

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | (1) 全局 `@app.errorhandler` 捕获异常返回 `{ "error": "描述" }`；(2) 所有 API 返回统一格式（成功返回数据，失败返回 `{ "error": "..." }`）；(3) 文件上传用 `uuid.uuid4()` 重命名；(4) 限制文件扩展名 `.xlsx`/`.xls`；(5) 限制上传大小 50MB；(6) logging 配置：按天轮转到 `logs/error.log`，记录请求路径和异常堆栈；(7) 可选启动密码（环境变量 `APP_PASSWORD`）。 |
| **验收标准** | 上传非 Excel 文件返回错误提示；超大文件被拒绝；错误日志写入文件 |
| **依赖** | 任务 1 |
| **产出文件** | `app.py` 中的全局错误处理和日志配置 |

---

### 第三层：前端界面（任务 12-20）

#### 任务 12：前端基础框架

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | 创建 `templates/index.html`，包含：(1) Vue 3 + Bootstrap 5 + Axios + Plotly.js 的 CDN 引用；(2) 三列 CSS Grid 响应式布局容器（左侧 250px、中间自适应、右侧 300px）；(3) Vue 实例初始化：`data` 包含 `currentSpace`, `spaces`, `datasets`, `charts`, `messages`, `aiConfigs` 等状态属性；(4) Axios 实例封装 `apiClient` 基路径 `/api`；(5) 全局 Toast 通知方法。 |
| **验收标准** | 页面加载后显示三列布局，Vue 实例无控制台错误 |
| **依赖** | 任务 1 |
| **产出文件** | `templates/index.html` 框架部分 |

#### 任务 13：左侧边栏（空间管理 UI）

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | (1) 空间列表：`v-for` 渲染，点击高亮并调用 `switchSpace(id)`；(2) "+ 新建空间"按钮：调用 `POST /api/spaces`；(3) 右键/悬停显示编辑/删除图标：重命名弹窗 + 删除确认；(4) "AI 配置管理"按钮 → 打开 AI 配置模态框；(5) "系统设置"按钮 → 打开备份/恢复/密码设置模态框；(6) 空间切换时触发加载关联的数据集、图表、聊天记录。 |
| **验收标准** | 点击空间切换正确、新建/重命名/删除正常 |
| **依赖** | 任务 3, 12, 19（AI 配置模态框） |
| **产出文件** | `templates/index.html` 侧边栏部分 |

#### 任务 14：中间主区域（空间标题 + 数据集区）

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | (1) 顶部栏：空间名称（可点击编辑→`PUT /api/spaces/<id>`）、AI 配置下拉框（`GET /api/ai-configs` + `POST /api/spaces/<id>/ai-config` 绑定）；(2) "数据集"卡片组：每个数据集显示名称、行数、上传时间，带删除按钮；(3) "上传 Excel"按钮 → 打开上传模态框（步骤①选择文件→步骤②显示工作表选择→步骤③数据预览 100 行+列类型标识→步骤④预处理选项：缺失值策略、采样方式、日期转换设置→确认上传调用预处理+保存）。 |
| **验收标准** | 上传 Excel 全流程（选文件→选 sheet→预览→预处理→确认）正常 |
| **依赖** | 任务 4, 12 |
| **产出文件** | `templates/index.html` 中间区域数据集部分 |

#### 任务 15：中间主区域（图表区）

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) 图表卡片网格（CSS Grid 自适应，每行 2-3 列）：每个卡片显示图表名称、类型标签、内嵌 Plotly 图表（通过 `GET /api/charts/<id>/render` 获取 HTML 后通过 `v-html` 嵌入）；(2) 卡片底部按钮组："编辑"（打开编辑模态框）| "删除"（确认弹窗调用 `DELETE`）| "导出图片"（调用导出接口）；(3) "新建图表"按钮 → 多步骤模态框：Step1 选择数据集 → Step2 选择图表类型（6 种带图标展示）→ Step3 选择 X 列（数值/日期）、Y 列（数值）、可选 Y2 列 → Step4 样式设置（标题、X/Y 轴标签、颜色主题）→ Step5 趋势线开关 → 确认创建；(4) 图表编辑模态框（复用创建步骤但预填充现有值）。 |
| **验收标准** | 创建 6 种图表正常显示，编辑保存后图表更新，删除正常 |
| **依赖** | 任务 5, 14 |
| **产出文件** | `templates/index.html` 中间区域图表部分 |

#### 任务 16：趋势分析面板

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **详细描述** | 紧随图表卡片下方显示趋势分析面板：(1) 展示 4 个指标：斜率（保留 4 位小数）、截距（4 位小数）、R²（4 位小数）、趋势方向（上升/下降/平稳，带颜色标识）；(2) 未来 3 期预测值表格（期数、X 值、预测 Y 值）；(3) "导出 CSV"按钮：下载包含原始数据点（X, Y）、拟合值（Y_pred）、未来预测值的 CSV 文件。面板仅在 `chart.trend_enabled = true` 时显示。 |
| **验收标准** | 趋势分析面板数据与图表中叠加的趋势线一致，导出 CSV 文件格式正确 |
| **依赖** | 任务 6, 15 |
| **产出文件** | `templates/index.html` 趋势分析面板部分 |

#### 任务 17：右侧 AI 助手面板

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) 顶部：数据集选择下拉（当前空间的数据集列表，可选"不选数据集"）；(2) 聊天消息区域（flex-grow 自适应高度，overflow-y: auto 滚动）：每条消息显示角色头像（用户/AI）、时间、内容（支持 Markdown 渲染，可使用 simple-markdown 或 v-html sanitized）；(3) 底部固定输入区：多行 `<textarea>`，Ctrl+Enter 或点击"发送"按钮发送消息，调用 `POST /api/chat`；(4) 加载中状态显示动画；(5) "清空对话"按钮调用 `DELETE /api/spaces/<id>/chat-history` 并刷新；(6) 空间切换时 `GET /api/spaces/<id>/chat-history` 加载历史消息。 |
| **验收标准** | 发送消息后 AI 回复展示在消息区，切换空间后历史正确加载 |
| **依赖** | 任务 8, 12, 14 |
| **产出文件** | `templates/index.html` 右侧面板部分 |

#### 任务 18：分析笔记面板

| 字段 | 内容 |
|------|------|
| **优先级** | 中 |
| **状态** | 未开始 |
| **详细描述** | (1) 在 AI 助手面板中增加"分析笔记"标签切换（聊天/笔记）；(2) 笔记列表：标题+创建时间+内容摘要（截取前 50 字），点击可查看完整内容；(3) 每条笔记有删除按钮；(4) AI 回复消息下方有"保存为笔记"按钮：自动填充标题（AI 回复前 20 字）和完整内容。 |
| **验收标准** | 从 AI 回复保存笔记、查看笔记列表、删除笔记均正常 |
| **依赖** | 任务 9, 17 |
| **产出文件** | `templates/index.html` 笔记面板部分 |

#### 任务 19：模态框

| 字段 | 内容 |
|------|------|
| **优先级** | 中 |
| **状态** | 未开始 |
| **详细描述** | (1) **AI 配置管理模态框**：Bootstrap 表格展示所有配置（名称/Base URL/模型/默认标记），每行有编辑和删除按钮；底部"新增配置"按钮展开表单（name, base_url, api_key, model, system_prompt textarea, max_tokens, temperature 滑块）；"设为默认"开关。(2) **备份/恢复模态框**：两个按钮—"下载备份"（调用 `GET /api/system/backup`）和"恢复备份"（文件选择器上传 .db 文件调用 `POST /api/system/restore`），带确认对话框提示会覆盖当前数据。 |
| **验收标准** | AI 配置增删改查正常，备份文件可下载和恢复 |
| **依赖** | 任务 7, 10, 12 |
| **产出文件** | `templates/index.html` 模态框部分 |

#### 任务 20：大数据优化

| 字段 | 内容 |
|------|------|
| **优先级** | 中 |
| **状态** | 未开始 |
| **详细描述** | (1) 前端数据预览时若 row_count > 100000 弹出采样提示框，引导用户输入采样数量进行随机采样（默认 50000）；(2) 图表创建时若数据点 > 5000，后端自动使用 `scattergl`/`scattergl`（WebGL 模式）替代普通 `scatter` 以提升渲染性能；(3) Plotly.js 配置 `responsive: true` 适配容器大小变化。 |
| **验收标准** | 大样本数据（>10万行）上传后弹出采样提示；图表渲染使用的 trace 类型为 scattergl |
| **依赖** | 任务 4, 5（修改） |
| **产出文件** | `templates/index.html` 采样提示逻辑；`app.py` 中图表类型选择逻辑 |

---

### 第四层：打包与测试（任务 21-22）

#### 任务 21：PyInstaller 打包

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | (1) 创建 `build.bat` 打包脚本：`pyinstaller --onefile --add-data "templates;templates" --add-data "uploads;uploads" --hidden-import ... app.py --name ExcelAny`，包含所有 hidden imports（sklearn, pandas, plotly 等所有子模块）。(2) 路径兼容性处理：使用 `get_app_dir()` 函数判断是否在打包环境（`sys._MEIPASS`），数据目录（数据库文件、uploads、logs）定位到 exe 所在目录（`os.path.dirname(sys.executable)`）。(3) 启动时使用 `webbrowser.open('http://127.0.0.1:5000')` 自动在默认浏览器打开。(4) `use_reloader=False` 确保打包后正常关闭。 |
| **验收标准** | `build.bat` 执行成功后生成单个 `ExcelAny.exe`，双击运行自动打开浏览器访问 |
| **依赖** | 所有后端任务 |
| **产出文件** | `build.bat`, 打包配置适配 |

#### 任务 22：集成测试与验证

| 字段 | 内容 |
|------|------|
| **优先级** | 高 |
| **状态** | 未开始 |
| **工作量预估** | 大 |
| **详细描述** | 启动应用后按以下场景逐一验证：<br>**场景 1**：新建 2 个空间 → 分别重命名 → 切换验证数据隔离。<br>**场景 2**：上传 Excel 文件（含多 sheet）→ 选择 sheet → 预览数据确认列类型正确 → 删除数据集。<br>**场景 3**：上传数据 → 创建 6 种图表各一个 → 验证渲染正确 → 编辑图表 → 删除图表。<br>**场景 4**：创建带有趋势线的散点/折线图 → 验证趋势线叠加、R²/斜率显示、未来 3 期预测值、CSV 导出。<br>**场景 5**：添加 AI 配置 → 设置默认 → 绑定到空间 → 发送消息（不选数据集）→ 发送消息（选数据集）→ 查看历史 → 清空对话。<br>**场景 6**：将 AI 回复保存为笔记 → 查看笔记列表 → 删除笔记。<br>**场景 7**：导出空间 ZIP → 验证 ZIP 内包含所有文件。<br>**场景 8**：下载数据库备份 → 修改数据 → 上传备份恢复 → 验证数据恢复。<br>**场景 9**：测试上传非 Excel 文件 → 验证错误提示。<br>**场景 10**：PyInstaller 打包 → 双击运行 exe → 自动打开浏览器 → 重复场景 1-9。 |
| **验收标准** | 全部 10 个场景通过 |
| **依赖** | 所有任务 |

---

## 四、项目目录结构（最终）

```
D:\code\other\excelAny\
├── app.py                  # Flask 应用（后端全部逻辑）
├── requirements.txt        # Python 依赖
├── build.bat               # PyInstaller 打包脚本
├── templates/
│   └── index.html          # 前端 SPA（Vue 3 + Bootstrap 5）
├── uploads/                # 上传的 Excel 文件（UUID 重命名）
├── logs/
│   └── error.log           # 错误日志（按天滚动）
├── backup/                 # 备份文件存放目录
└── excelany.db             # SQLite 数据库（自动生成）
```

## 五、技术决策记录

| 决策点 | 选择 | 理由 |
|--------|------|------|
| Python 版本 | 3.9+ | scikit-learn 最低要求 |
| 前端框架 | Vue 3 CDN + Options API | 单页面无需构建工具，Options API 更易组织复杂组件 |
| CSS 方案 | Bootstrap 5 + CSS Grid | 响应式布局、模态框、表格、卡片等组件开箱即用 |
| 图表库 | Plotly.js | 支持交互式图表、WebGL、导出图片 |
| 数据库 | SQLite (SQLAlchemy) | 桌面应用场景，无部署数据库服务需求 |
| AI API 协议 | OpenAI 兼容格式 | 兼容性最广，支持国内大多数 LLM API |
| 打包 | PyInstaller --onefile | 单 exe 便于分发 |
| 前端渲染 | 后端生成 Plotly JSON → 前端 Plotly.newPlot | 避免前端传大量数据，降低内存占用 |

## 六、依赖清单（requirements.txt）

```
Flask==3.1.0
Flask-CORS==5.0.1
SQLAlchemy==2.0.36
pandas==2.2.3
numpy==2.2.3
scikit-learn==1.6.1
openpyxl==3.1.5
requests==2.32.3
plotly==5.25.0
PyInstaller==6.11.1
```